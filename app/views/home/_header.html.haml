- if current_user.admin == true
  .header
    .header__left
      = link_to(root_path, class: "header__left--btn") do
        %i.fa.fa-coffee.header__left--btn__icon
    .header__center
      .header__center__text
        = link_to(root_path, class: "header__center--btn") do
          This is Me
      .header__right
  = link_to "質問登録", new_admin_question_path
  = link_to "ログアウト", destroy_user_session_path, method: :delete
  = link_to "answer_new", past_new_answers_path, method: :get
- else
  .header
    .header__left
      = link_to(root_path, class: "header__left--btn") do
        %i.fa.fa-coffee.header__left--btn__icon
    .header__center
      .header__center__text
        = link_to(root_path, class: "header__center--btn") do
          This is Me
    .header__right
  = link_to "ログアウト", destroy_user_session_path, method: :delete
  = link_to "answer_new", past_new_answers_path, method: :get
